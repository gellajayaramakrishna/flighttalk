createCabinApp({
  mode: 'airline',
  tokenKey: 'ft_token_airline',
  titles: {
    lobby: ['Cabin chat', 'Flight-only channel'],
    people: ['Passengers', 'Opt-in discovery only'],
    rooms: ['Rooms', 'Create, join, host, kick'],
    files: ['Files', 'Temporary cabin storage'],
    games: ['Games', 'Server-authoritative state']
  }
});
